logging:
  level: "DEBUG"
  type: "json"

storage:
  type: "db"
  db:
    endpoint: "localhost:5432"
    user: "postgres"
    database: "events"
    pass: "1234"

http:
  host: "localhost"
  port: "8888"